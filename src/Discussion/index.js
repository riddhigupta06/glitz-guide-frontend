import React from "react"
import { Box, Button, Spinner, Heading, Badge, IconButton, Tooltip } from "@chakra-ui/react";

export default function Discussion() {
    return (
        <Box padding={3}>
            <Heading as={'h6'} size={'lg'}>Influencer Discussion Board</Heading>
        </Box>
    )
}